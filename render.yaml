services:
  - type: web
    name: keep-talking-3d
    env: node
    plan: free
    branch: master
    autoDeploy: true
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.x
